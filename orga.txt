📑 DOCUMENT D'ORGANISATION : OMNIHUB V2
1. Partie FRONT-END (Amin)

Objectif : Concevoir une interface utilisateur (UI) moderne, réactive et capable d'afficher des données complexes (priorités, dates, etc.). 

A. Structure Générale & Layout

La Grande Barre de Titre (Navbar) : Créer un bandeau fixe en haut avec le nom "OmniHub", un slogan dynamique et une horloge en temps réel. 


Le Tableau de Bord (Dashboard) : Utiliser une grille à 3 colonnes (CSS Grid) pour organiser les zones : To-Do (Tâches), Contacts, et Notes. 


Les Barres de Recherche : Placer un champ de texte avec une icône de loupe en haut de chaque colonne pour filtrer les données en temps réel. 


Le Design "Flou" (Glassmorphism) : Appliquer un effet de flou d'arrière-plan (backdrop-filter: blur) derrière les fenêtres modales pour un aspect premium. 

B. Les Composants Atomiques (Réutilisables) 

Ligne "Contact" : Carte affichant les initiales, le Nom, le Prénom et le Numéro. Prévoir des boutons iconographiques pour "Modifier" et "Supprimer". 

Ligne "Note" : Affichage du titre en gras avec un aperçu du contenu. Inclure la date de dernière modification. 


Ligne "Tâche" : Inclure une checkbox, le texte de la tâche et une pastille de couleur indiquant la priorité (Rouge: High, Jaune: Medium, Vert: Low). 

C. Menus de Saisie & Modales 


Formulaire Contact : Champs pour Nom, Prénom, Numéro (format 10 chiffres) et Email. 


Formulaire Note : Champ Titre et une zone de texte large (Textarea). 


Boutons d'Action : Paires de boutons standardisées "Enregistrer / Annuler" et "Confirmer / Supprimer". 

A. Structure des Composants (Props & Data Flow)
Chaque ligne ou carte doit être conçue comme un composant React indépendant.


Composant TodoItem:

Props attendues : task (string), priority (string), isCompleted (boolean), onToggle (fonction), onDelete (fonction).

Logique : La couleur de fond change selon la priority. La case à cocher déclenche onToggle.


Composant ContactCard:

Props attendues : firstname, surname, phonenumber, email, onEdit, onDelete.

Logique : Affiche les informations de manière élégante. Si email est absent, le champ ne s'affiche pas.


Composant NoteCard:

Props attendues : title, content, createdAt, onDelete.


Logique : Affiche le titre en gras et tronque le contenu s'il dépasse 100 caractères pour garder l'alignement des colonnes.

2. Partie BACK-END & INTÉGRATION (Rayhan)

Objectif : Assurer la persistance des données, la sécurité des échanges et le lien entre l'interface et le serveur. 

A. Le Serveur & API (Déjà Opérationnel) 


Routes RESTful : Gestion complète du CRUD (Create, Read, Update, Delete) pour les trois modules. 


Stockage JSON : Utilisation de fichiers sécurisés (notes.json, todos.json, contacts.json) pour sauvegarder les données. 


Validation Joi : Système de filtrage strict empêchant l'envoi de données invalides (ex: mail mal formé, numéro incorrect, titre trop court). 

B. La Logique de Liaison (Le "Bridge") 

Connexion Axios : Coder les appels asynchrones vers le serveur. Le Frontend doit pouvoir récupérer les listes au chargement de la page (useEffect). 

Gestion des États (State Management) : Gérer l'affichage dynamique des données : si une note est supprimée sur le serveur, elle doit disparaître instantanément du Front.


Gestion des Modales : Coder la logique d'ouverture/fermeture des formulaires au clic sur les boutons d'ajout ou de modification. 

C. Traitement des Données (Data Intelligence)

Moteur de recherche : Créer un filtre JavaScript côté client qui réduit la liste affichée dès que l'utilisateur tape une lettre. 

Algorithme de Tri : Les tâches doivent être automatiquement triées par importance (Priority) avant d'être envoyées au Frontend.